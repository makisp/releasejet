name: Release Notes
on:
  push:
    tags:
      - '**'
jobs:
  release-notes:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - run: npm install -g @makispps/releasejet
      - run: releasejet generate --tag "${{ github.ref_name }}" --publish
        env:
          RELEASEJET_TOKEN: ${{ secrets.RELEASEJET_TOKEN }}

# --- Pro Support (uncomment if using @releasejet/pro) ---
# Add RELEASEJET_PRO_TOKEN to your repo secrets with your rlj_ license key.
#
# Replace the "npm install" step above with:
#     - name: Configure Pro registry
#       run: |
#         echo "@releasejet:registry=https://npm.releasejet.dev/" >> ~/.npmrc
#         echo "//npm.releasejet.dev/:_authToken=${RELEASEJET_PRO_TOKEN}" >> ~/.npmrc
#       env:
#         RELEASEJET_PRO_TOKEN: ${{ secrets.RELEASEJET_PRO_TOKEN }}
#     - run: npm install -g @makispps/releasejet @releasejet/pro
# ---
